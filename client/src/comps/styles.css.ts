import { style } from "@vanilla-extract/css";

const wrapper = style({
  padding: 32,
});

const preLine = style({
  whiteSpace: "pre-line",
});

const styles = {
  wrapper,
  preLine,
};

export default styles;
